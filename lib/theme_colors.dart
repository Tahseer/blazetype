import 'package:flutter/material.dart';

class ThemeColors {
  static const Color green =
      Color.fromARGB(255, 201, 243, 87); // Fresh green for buttons (updated)
  static const Color red = Color(0xFFF1628C); // Keeping the original red
  static const Color yellow =
      Color.fromARGB(255, 137, 13, 186); // Keeping the original yellow
}
